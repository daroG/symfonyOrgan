{% extends 'base.html.twig' %}


{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    <button type="submit">Add Tag!</button>
    {{ form_end(form) }}


    <a href="{{ path('song_create') }}">Create new song</a>

{% endblock %}